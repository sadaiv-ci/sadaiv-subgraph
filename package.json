{
  "name": "graph",
  "license": "UNLICENSED",
  "version": "0.0.1",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service tusharojha/sadaiv",
    "create-local": "graph create --node http://localhost:8020/ sadaiv-ci/graph",
    "remove-local": "graph remove --node http://localhost:8020/ sadaiv-ci/graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sadaiv-ci/graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
